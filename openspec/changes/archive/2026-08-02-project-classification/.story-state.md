# Story state — project-classification
- add-generate-project-flag: added required --project flag to generate (missing flag exit 2, missing value/flag-as-value exit 2, rejected on other subcommands, usage/help updated)
- project-scoped-seed: write-seed now emits Project {name}, Change {name, project}, Story {id, change, project}, BELONGS_TO + three-property HAS_STORY/DEPENDS_ON; header has project line
- regenerate-seed-goldens: golden story-seed.cypher regenerated with --project fixture-project; parity test passes --project; stories.md golden unchanged; all 74 assertions green
- parser-and-seed-shape-tests: added project-flag-parsing (6 cases) + seed-shape (10 assertions) deftests; 97 assertions green
- skill-classify-project-docs: SKILL.md generate gains --project <name>; Phase 1b Classify the project (D1 derivation, D4 type/techStack/repoUrl contracts, full-overwrite SET with null-clearing)
- skill-project-scoped-queries: all Cypher snippets project-scoped (resume guard, poll, transitions {id,change,project}, blocked/completion, schema ref with Project/BELONGS_TO)
- command-agents-project-updates: opsx-story derives canonical project from git origin, passes --project, adds classify step; AGENTS.md invocation includes --project <name>
- full-verification: both suites green (125 assertions); manual CLI checks pass (project-scoped seed, exit 2 without --project, -h required); doc greps pass; graph idempotency verified (seed run twice, no duplicates)
