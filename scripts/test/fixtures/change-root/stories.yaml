change: fixture-change
stories:
  - id: scaffold
    title: Scaffold the module
    description: |
      Create the module scaffold.
      Quotes "quoted", backslash \, unicode café 日本語.
    acceptanceCriteria:
      - 'Scaffold exists with "quoted" files'
      - "Handles café and 日本語\non the second line"
    dependsOn: []
    taskRefs:
      - "1.1 Create the \"module\" scaffold"
      - "1.2 Install deps: café & 日本語"
      - "1.3 Use backslash \\ as separator"

  - id: core
    title: Implement core behaviors
    description: "Engine core with \"quotes\", backslash \\, and café 日本語\nand a newline"
    acceptanceCriteria:
      - "Events fire: \"done\""
      - "Unicode é, 東京, and \n newline"
    dependsOn:
      - scaffold
    taskRefs:
      - "2.1 Implement the engine"
      - "2.2 Wire \"events\" end-to-end"
      - "2.3 Ship the release"
